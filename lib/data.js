const { KHITROV_HOLIDAY_TOKEN, PRETTY_LOGGING } = process.env;

const listOfCommands = [{ command: '/start', description: 'List of commands' }];

module.exports = {
  KHITROV_HOLIDAY_TOKEN,
  PRETTY_LOGGING,
  listOfCommands,
};
